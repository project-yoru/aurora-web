- projects#show: auto refresh, or push the update of distribution building
- optimize speed: insight test via google admin
- optimize speed, cdn for assets, google font for fonts

- project config job, dont like distribution build job, dont have a `halt current and redo` function

- use uuid as identifier
